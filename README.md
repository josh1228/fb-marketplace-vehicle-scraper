# fb-marketplace-vehicle-scraper

## Project Documentation

This project provides an educational implementation of a vehicle scraper for Facebook Marketplace with Heroku deployment support.

### Setup
- Clone the repository
- Install dependencies using `pip install -r requirements.txt`

### Heroku Deployment
1. Create a new Heroku app.
2. Push the code to Heroku using `git push heroku main`.

